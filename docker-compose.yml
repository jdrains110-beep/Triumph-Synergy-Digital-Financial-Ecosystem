# ==========================================================================
# TRIUMPH SYNERGY DIGITAL FINANCIAL ECOSYSTEM
# ==========================================================================
# Docker Desktop deployment — runs ALONGSIDE Pi Network node
# Container prefix: triumph-*
#
# USAGE:
#   Mainnet:  docker compose up -d
#   Testnet:  docker compose --env-file .env.testnet up -d
#   Upgrade:  docker compose pull; docker compose up -d --build
#   Logs:     docker compose logs -f
#   Stop:     docker compose down
# ==========================================================================

name: triumph-synergy

x-common: &common
  restart: unless-stopped
  logging:
    driver: json-file
    options:
      max-size: "5m"
      max-file: "2"
  # ── Memory overflow protection: container dies before it kills WSL2 ──
  memswap_limit: 0           # No swap — OOM-kill the container, don't swap (prevents I/O death spiral)
  oom_kill_disable: false     # NEVER disable OOM killer — let Docker kill runaway containers
  labels:
    org.pinetwork.project: "triumph-synergy"
    org.pinetwork.central-node: "GA6Z5STFJZPBDQT5VZSDUTCKLXXB626ONTLRWBJAWYKLH4LKPIZCGL7V"
    org.triumph.blockchain.powered-by: "Pi Network"
    org.triumph.consensus.protocol: "Stellar SCP"
    org.triumph.horizon.layer: "enabled"
    org.triumph.ecosystem.class: "sovereign-quantum-financial"
    org.triumph.positioning: "rival-and-superior-to-usd"
    org.triumph.real-world-utility: "20-plus-sectors"
    org.triumph.gateway: "universal-integration-gateway"
    org.triumph.owner.name: "Jeremiah Joel Drains"
    org.triumph.owner.organization: "Triumph-Synergy"
    org.triumph.governance.final-authority: "owner-approved"
    org.triumph.legal.compliance.required: "true"

x-db-env: &db-env
  POSTGRES_URL: postgresql://postgres:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD required}@triumph-postgres:5432/${POSTGRES_DB:-triumph_synergy}
  REDIS_URL: redis://triumph-redis:6379
  # Supabase keys (set in .env or secrets manager for production)
  NEXT_PUBLIC_SUPABASE_URL: ${NEXT_PUBLIC_SUPABASE_URL:-https://your-project.supabase.co}
  NEXT_PUBLIC_SUPABASE_ANON_KEY: ${NEXT_PUBLIC_SUPABASE_ANON_KEY:-your-anon-key}
  SUPABASE_SERVICE_ROLE_KEY: ${SUPABASE_SERVICE_ROLE_KEY:-your-service-role-key}
  SUPABASE_DB_URL: ${SUPABASE_DB_URL:-postgresql://postgres:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD required}@triumph-postgres:5432/${POSTGRES_DB:-triumph_synergy}}

x-pi-env: &pi-env
  PI_NETWORK_MODE: ${PI_NETWORK_MODE:-mainnet}
  # Active key resolves to mainnet or testnet depending on PI_NETWORK_MODE
  PI_API_KEY: ${PI_API_KEY:?PI_API_KEY required}
  PI_INTERNAL_API_KEY: ${PI_INTERNAL_API_KEY:?PI_INTERNAL_API_KEY required}
  # Both keys embedded so network-aware services can select the right one at runtime
  PI_MAINNET_API_KEY: ${PI_MAINNET_API_KEY:?PI_MAINNET_API_KEY required}
  PI_TESTNET_API_KEY: ${PI_TESTNET_API_KEY:?PI_TESTNET_API_KEY required}
  PI_API_SECRET: ${PI_API_SECRET:?PI_API_SECRET required}
  PI_API_URL: ${PI_API_URL:-https://api.minepi.com}
  NEXT_PUBLIC_PI_APP_ID: ${NEXT_PUBLIC_PI_APP_ID:-triumph-synergy}
  NEXT_PUBLIC_PI_SANDBOX: ${NEXT_PUBLIC_PI_SANDBOX:-false}
  PI_SUPERNODE_ADDRESS: GA6Z5STFJZPBDQT5VZSDUTCKLXXB626ONTLRWBJAWYKLH4LKPIZCGL7V
  PI_SUPERNODE_MODE: "true"
  STELLAR_HORIZON_URL: ${STELLAR_HORIZON_URL:-https://api.mainnet.minepi.com}
  STELLAR_NETWORK_PASSPHRASE: ${STELLAR_NETWORK_PASSPHRASE:-Pi Network}
  CENTRAL_NODE_URL: http://triumph-central-node:11626
  CENTRAL_NODE_PUBLIC_KEY: GA6Z5STFJZPBDQT5VZSDUTCKLXXB626ONTLRWBJAWYKLH4LKPIZCGL7V
  # Pi Node accessible via pi-bridge network (container name) and host
  PI_NODE_HOST: ${PI_NODE_CONTAINER:-testnet2}
  PI_NODE_PORT: ${PI_NODE_PORT:-31402}
  PI_NODE_API_PORT: ${PI_NODE_API_PORT:-8000}
  PI_NODE_PEER_PORT: ${PI_NODE_PEER_PORT:-31402}

services:
  # ========================================================================
  # INFRASTRUCTURE
  # ========================================================================

  postgres:
    image: postgres:16-alpine
    container_name: triumph-postgres
    <<: *common
    mem_limit: 256m
    mem_reservation: 128m
    cpus: 0.4
    oom_score_adj: -500          # Protect database — last to OOM-kill
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-triumph_synergy}
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD required}
    # Internal only — no host port exposure
    expose:
      - "5432"
    volumes:
      - triumph_pg_data:/var/lib/postgresql/data
      - ./docker/postgres/init:/docker-entrypoint-initdb.d:ro
    networks:
      - triumph-net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d ${POSTGRES_DB:-triumph_synergy}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s

  redis:
    image: redis:7-alpine
    container_name: triumph-redis
    <<: *common
    mem_limit: 128m
    mem_reservation: 64m
    cpus: 0.2
    oom_score_adj: -500          # Protect cache — last to OOM-kill
    # Internal only — no host port exposure
    expose:
      - "6379"
    volumes:
      - triumph_redis_data:/data
      - ./docker/redis/redis.conf:/usr/local/etc/redis/redis.conf:ro
    networks:
      - triumph-net
    command: redis-server /usr/local/etc/redis/redis.conf
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  # ========================================================================
  # PI BRIDGE CONNECTOR — Central Node ↔ Pi Node (testnet2) live link
  # Polls testnet2:8000 (Horizon) every 5s, publishes to Redis, routes txs
  # ========================================================================

  pi-bridge-connector:
    build:
      context: docker/pi-bridge-connector
    container_name: triumph-pi-bridge-connector
    <<: *common
    restart: on-failure:10            # override: stop crash-looping if Pi Node desktop is offline (prevents CPU storm)
    mem_limit: 192m
    mem_reservation: 96m
    cpus: 0.2
    environment:
      <<: [*db-env, *pi-env]
      PORT: "8092"
      PI_NODE_HOST: ${PI_NODE_CONTAINER:-testnet2}
      PI_NODE_API_PORT: 8000
      PI_NODE_PEER_PORT: ${PI_NODE_PEER_PORT:-31402}
      PI_NODE_FALLBACK_URL: http://host.docker.internal:31401
      PI_NODE_PUBLIC_FALLBACK_URL: https://api.testnet.minepi.com
      PI_POLL_RETRIES: "4"
      PI_POLL_RETRY_DELAY_S: "1.5"
      POLL_INTERVAL_S: "6"
      CENTRAL_NODE_URL: http://triumph-central-node:11626
      QUANTUM_SHIELD_URL: http://triumph-quantum-shield:8094
      SOVEREIGN_PQ_ENFORCE: "true"
      STELLAR_CORE_VERSION_OVERRIDE: "5.4"
      PI_NODE_VERSION_DISPLAY: "5.4"
    expose:
      - "8092"
    networks:
      - triumph-net
      - pi-bridge
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8092/health', timeout=10)"]
      interval: 15s
      timeout: 15s
      retries: 5
      start_period: 45s

  # ========================================================================
  # PI CENTRAL NODE — Stellar Core Integration
  # ========================================================================

  central-node:
    build:
      context: .
      dockerfile: docker/central-node/Dockerfile
    container_name: triumph-central-node
    <<: *common
    mem_limit: 256m
    mem_reservation: 128m
    cpus: 0.4
    environment:
      <<: [*db-env, *pi-env]
      NODE_TYPE: central
      NODE_PRIORITY: supreme
      STELLAR_CORE_HTTP_PORT: 11626
      STELLAR_CORE_PEER_PORT: 11625
      PI_API_URL: ${PI_API_URL:-https://api.minepi.com}
      # Prefer bridge proxy endpoint for resilient access to local Pi node.
      STELLAR_HORIZON_URL: http://${PI_NODE_CONTAINER:-testnet2}:${PI_NODE_API_PORT:-8000}
      PI_LOCAL_HORIZON: http://triumph-pi-bridge-connector:8092/pi-node
      PI_INTERNAL_HORIZON_URL: http://triumph-pi-bridge-connector:8092/pi-node
      PI_BRIDGE_URL: http://triumph-pi-bridge-connector:8092
      SCP_AUTO_UPGRADE: "true"
      SCP_SYNC_INTERVAL_MS: 30000
      NODE_OPTIONS: "--max-old-space-size=384"
      PI_NODE_VERSION_DISPLAY: "5.4"
      DATABASE_URL: postgresql://postgres:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD required}@triumph-postgres:5432/${POSTGRES_DB:-triumph_synergy}
    # Internal only — no host port exposure
    expose:
      - "11625"
      - "11626"
    volumes:
      - triumph_central_data:/app/data
      - triumph_stellar_data:/opt/stellar/data
    networks:
      - triumph-net
      - pi-bridge
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      pi-bridge-connector:
        condition: service_started    # service_started (not service_healthy) — pi-bridge connects to external Pi Node;
                                      # if Pi Node desktop is offline, don't block entire ecosystem startup
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:11626/info"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 30s

  # ========================================================================
  # NEXT.JS APPLICATION — Main Ecosystem UI & API
  # ========================================================================

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: triumph-app
    <<: *common
    mem_limit: 384m
    mem_reservation: 192m
    cpus: 0.6
    ports:
      - "3000:3000"
    environment:
      <<: [*db-env, *pi-env]
      NODE_ENV: production
      DOCKER_BUILD: "true"
      AUTH_SECRET: ${AUTH_SECRET:?AUTH_SECRET required}
      AUTH_TRUST_HOST: "true"
      NEXTAUTH_SECRET: ${NEXTAUTH_SECRET:?NEXTAUTH_SECRET required}
      NEXTAUTH_URL: ${NEXTAUTH_URL:-http://localhost:3000}
      FRONTEND_URL: ${FRONTEND_URL:-http://localhost:3000}
      BACKEND_URL: ${BACKEND_URL:-http://localhost:3000/api}
      INTERNAL_PI_MULTIPLIER: ${INTERNAL_PI_MULTIPLIER:-1.5}
      INTERNAL_PI_MIN_VALUE: ${INTERNAL_PI_MIN_VALUE:-10.0}
      EXTERNAL_PI_MIN_VALUE: ${EXTERNAL_PI_MIN_VALUE:-1.0}
      DUAL_VALUE_URL: http://triumph-dual-value-engine:8093
    security_opt:
      - no-new-privileges:true
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      central-node:
        condition: service_started    # service_started — Next.js handles central-node unavailability gracefully via retry logic
    networks:
      - triumph-net
      - pi-bridge
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3000/api/health"]
      interval: 20s
      timeout: 10s
      retries: 5
      start_period: 60s

  # ========================================================================
  # PI TRANSACTION ENGINE — High-Throughput Processing
  # ========================================================================

  transaction-engine:
    build:
      context: .
      dockerfile: docker/transaction-engine/Dockerfile
    container_name: triumph-transaction-engine
    <<: *common
    mem_limit: 128m
    mem_reservation: 64m
    cpus: 0.2
    environment:
      <<: [*db-env, *pi-env]
      ENGINE_MODE: hyper
      NODE_OPTIONS: "--max-old-space-size=96"
      DATABASE_URL: postgresql://postgres:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD required}@triumph-postgres:5432/${POSTGRES_DB:-triumph_synergy}
      QUANTUM_SHIELD_URL: http://triumph-quantum-shield:8094
      TRANSACTION_REQUIRE_PQ_SIGNATURE: "true"
    volumes:
      - triumph_tx_data:/app/data
    networks:
      - triumph-net
      - pi-bridge
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      central-node:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 30s

  # ========================================================================
  # PI TRILLION VAULT — Secure Storage
  # ========================================================================

  vault:
    build:
      context: .
      dockerfile: docker/vault/Dockerfile
    container_name: triumph-vault
    <<: *common
    mem_limit: 128m
    mem_reservation: 64m
    cpus: 0.2
    environment:
      <<: [*db-env, *pi-env]
      VAULT_MODE: trillion
      QUANTUM_ENCRYPTION: "true"
      NODE_OPTIONS: "--max-old-space-size=96"
      DATABASE_URL: postgresql://postgres:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD required}@triumph-postgres:5432/${POSTGRES_DB:-triumph_synergy}
    volumes:
      - triumph_vault_data:/app/data
    networks:
      - triumph-net
      - pi-bridge
    depends_on:
      central-node:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8081/health"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 30s

  # ========================================================================
  # PI SMART CONTRACTS — Execution Engine
  # ========================================================================

  smart-contracts:
    build:
      context: .
      dockerfile: docker/smart-contracts/Dockerfile
    container_name: triumph-smart-contracts
    <<: *common
    mem_limit: 128m
    mem_reservation: 64m
    cpus: 0.2
    environment:
      <<: [*db-env, *pi-env]
      EXECUTION_CHANNELS: 10000
      DUAL_VALUE_URL: http://triumph-dual-value-engine:8093
      QUANTUM_SHIELD_URL: http://triumph-quantum-shield:8094
      PI_BRIDGE_URL: http://triumph-pi-bridge-connector:8092
      CENTRAL_NODE_URL: http://triumph-central-node:11626
      CONTRACTS_URL: http://triumph-smart-contracts:8082
      PI_INTERNAL_RATE: "314159.0"
      PI_EXTERNAL_RATE: "314.159"
      PI_INTERNAL_MULTIPLIER: "1000.0"
      NODE_OPTIONS: "--max-old-space-size=96"
      DATABASE_URL: postgresql://postgres:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD required}@triumph-postgres:5432/${POSTGRES_DB:-triumph_synergy}
    volumes:
      - triumph_contracts_data:/app/data
    networks:
      - triumph-net
      - pi-bridge
    depends_on:
      central-node:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8082/health"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 30s

  # ========================================================================
  # PI SCP UPGRADER — Auto Protocol Sync
  # ========================================================================

  scp-upgrader:
    build:
      context: .
      dockerfile: docker/scp-upgrader/Dockerfile
    container_name: triumph-scp-upgrader
    <<: *common
    mem_limit: 64m
    mem_reservation: 32m
    cpus: 0.2
    environment:
      <<: [*db-env, *pi-env]
      AUTO_UPGRADE: "true"
      ZERO_DOWNTIME: "true"
      ROLLBACK_ON_FAILURE: "true"
      VERSION_CHECK_INTERVAL_MS: 60000
      PARAMETER_SYNC_INTERVAL_MS: 30000
      # KEY FIX: use local Pi node for SCP data instead of external internet
      PI_MAINNET_HORIZON: https://api.mainnet.minepi.com
      PI_TESTNET_HORIZON: https://api.testnet.minepi.com
      PI_LOCAL_HORIZON: http://${PI_NODE_CONTAINER:-testnet2}:${PI_NODE_API_PORT:-8000}
      PI_BRIDGE_URL: http://triumph-pi-bridge-connector:8092
      NETWORK_TYPE: ${PI_NETWORK_MODE:-mainnet}
      NODE_OPTIONS: "--max-old-space-size=48"
    volumes:
      - triumph_scp_data:/app/data
    networks:
      - triumph-net
      - pi-bridge
    depends_on:
      central-node:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8083/health"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 30s

  # ========================================================================
  # PI PAYMENT PROCESSOR — Queue-Based Payment Handling
  # ========================================================================

  payment-processor:
    build:
      context: ./services/payment-processor
      dockerfile: Dockerfile
    container_name: triumph-payment-processor
    <<: *common
    mem_limit: 96m
    mem_reservation: 48m
    cpus: 0.25
    environment:
      <<: [*db-env, *pi-env]
      WORKER_THREADS: 2
      NODE_OPTIONS: "--max-old-space-size=64"
      QUANTUM_SHIELD_URL: http://triumph-quantum-shield:8094
      PAYMENT_REQUIRE_PQ_SIGNATURE: "true"
    networks:
      - triumph-net
      - pi-bridge
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8084/health"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 30s

  # ========================================================================
  # NGINX — Reverse Proxy & Load Balancer
  # ========================================================================

  nginx:
    image: nginx:alpine
    container_name: triumph-nginx
    <<: *common
    mem_limit: 128m
    mem_reservation: 32m
    cpus: 0.2
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./certs/nginx/server-cert.pem:/etc/nginx/certs/server-cert.pem:ro
      - ./certs/nginx/server-key.pem:/etc/nginx/certs/server-key.pem:ro
    depends_on:
      app:
        condition: service_started
      quantum-shield:
        condition: service_started    # service_started — quantum-shield's 60s startup should not delay nginx
    networks:
      - triumph-net
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:80/health"]
      interval: 15s
      timeout: 10s
      retries: 3
      start_period: 30s

  # ========================================================================
  # MARKET DATA SERVICE — Live Pi Horizon data relay
  # ========================================================================

  market-data:
    build:
      context: .
      dockerfile: docker/market-data/Dockerfile
    container_name: triumph-market-data
    <<: *common
    mem_limit: 96m
    mem_reservation: 48m
    cpus: 0.2
    environment:
      <<: [*db-env, *pi-env]
      MARKET_POLL_MS: 5000
      # KEY FIX: market data reads live Pi ledger via bridge connector + Pi price from ML engine
      ML_ENGINE_URL: http://triumph-ml-engine:8090
      PI_BRIDGE_URL: http://triumph-pi-bridge-connector:8092
      DUAL_VALUE_URL: http://triumph-dual-value-engine:8093
      QUANTUM_SHIELD_URL: http://triumph-quantum-shield:8094
      STELLAR_HORIZON_URL: http://${PI_NODE_CONTAINER:-testnet2}:${PI_NODE_API_PORT:-8000}
      PI_INTERNAL_RATE: "314159.0"
      PI_EXTERNAL_RATE: "314.159"
      PI_INTERNAL_MULTIPLIER: "1000.0"
      NODE_OPTIONS: "--max-old-space-size=64"
      DATABASE_URL: postgresql://postgres:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD required}@triumph-postgres:5432/${POSTGRES_DB:-triumph_synergy}
    networks:
      - triumph-net
      - pi-bridge
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8085/health"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 20s

  # ========================================================================
  # BLOCKCHAIN ORACLE — Pi ledger event stream → Redis pub/sub
  # ========================================================================

  blockchain-oracle:
    build:
      context: .
      dockerfile: docker/blockchain-oracle/Dockerfile
    container_name: triumph-blockchain-oracle
    <<: *common
    mem_limit: 96m
    mem_reservation: 48m
    cpus: 0.25
    environment:
      <<: [*db-env, *pi-env]
      CONTRACTS_URL: http://triumph-smart-contracts:8082
      # KEY FIX: oracle reads live Pi ledger events via bridge connector
      PI_BRIDGE_URL: http://triumph-pi-bridge-connector:8092
      DUAL_VALUE_URL: http://triumph-dual-value-engine:8093
      QUANTUM_SHIELD_URL: http://triumph-quantum-shield:8094
      STELLAR_HORIZON_URL: http://${PI_NODE_CONTAINER:-testnet2}:${PI_NODE_API_PORT:-8000}
      PI_INTERNAL_RATE: "314159.0"
      PI_EXTERNAL_RATE: "314.159"
      PI_INTERNAL_MULTIPLIER: "1000.0"
      NODE_OPTIONS: "--max-old-space-size=64"
      DATABASE_URL: postgresql://postgres:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD required}@triumph-postgres:5432/${POSTGRES_DB:-triumph_synergy}
    networks:
      - triumph-net
      - pi-bridge
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      smart-contracts:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8086/health"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 30s

  # ========================================================================
  # COMPLIANCE ENGINE — KYC / AML / GDPR / MiCA screening
  # ========================================================================

  compliance:
    build:
      context: .
      dockerfile: docker/compliance/Dockerfile
    container_name: triumph-compliance
    <<: *common
    mem_limit: 96m
    mem_reservation: 48m
    cpus: 0.25
    environment:
      <<: [*db-env, *pi-env]
      AML_THRESHOLD_PI: ${AML_THRESHOLD_PI:-10000}
      BLOCKED_ADDRESSES: ${BLOCKED_ADDRESSES:-}
      NODE_OPTIONS: "--max-old-space-size=64"
      DATABASE_URL: postgresql://postgres:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD required}@triumph-postgres:5432/${POSTGRES_DB:-triumph_synergy}
    networks:
      - triumph-net
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8087/health"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 20s

  # ========================================================================
  # DEX ENGINE — Pi token decentralized exchange
  # ========================================================================

  dex:
    build:
      context: .
      dockerfile: docker/dex/Dockerfile
    container_name: triumph-dex
    <<: *common
    mem_limit: 128m
    mem_reservation: 64m
    cpus: 0.2
    environment:
      <<: [*db-env, *pi-env]
      DUAL_VALUE_URL: http://triumph-dual-value-engine:8093
      PI_INTERNAL_RATE: "314159.0"
      PI_EXTERNAL_RATE: "314.159"
      PI_INTERNAL_MULTIPLIER: "1000.0"
      NODE_OPTIONS: "--max-old-space-size=96"
      DATABASE_URL: postgresql://postgres:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD required}@triumph-postgres:5432/${POSTGRES_DB:-triumph_synergy}
    networks:
      - triumph-net
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      market-data:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8088/health"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 25s

  # ========================================================================
  # ML ENGINE — IsolationForest anomaly, GBM fraud, Ridge price, RSI sentiment
  # ========================================================================

  ml-engine:
    build:
      context: docker/ml-engine
    container_name: triumph-ml-engine
    <<: *common
    mem_limit: 384m
    mem_reservation: 192m
    cpus: 0.4
    environment:
      <<: [*db-env, *pi-env]
      PORT: "8090"
      PYTHONUNBUFFERED: "1"
      PI_INTERNAL_RATE: "314159.0"
      PI_EXTERNAL_RATE: "314.159"
      PI_INTERNAL_MULTIPLIER: "1000.0"
      DUAL_VALUE_URL: http://triumph-dual-value-engine:8093
    expose:
      - "8090"
    networks:
      - triumph-net
    depends_on:
      redis:
        condition: service_healthy
      market-data:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8090/health"]
      interval: 20s
      timeout: 15s
      retries: 6
      start_period: 90s          # extended: Python ML model loading is slow under CPU contention

  # ========================================================================
  # CREDIT ENGINE — PiCredit Score™ + Bureau Integration
  # Equifax | Experian | TransUnion | FICO | VantageScore
  # ========================================================================

  credit-engine:
    build:
      context: docker/credit-engine
    container_name: triumph-credit-engine
    <<: *common
    mem_limit: 128m
    mem_reservation: 64m
    cpus: 0.2
    environment:
      <<: [*db-env, *pi-env]
      PORT: "8091"
      ML_ENGINE_URL: http://triumph-ml-engine:8090
      DUAL_VALUE_URL: http://triumph-dual-value-engine:8093
      QUANTUM_SHIELD_URL: http://triumph-quantum-shield:8094
      PI_INTERNAL_RATE: "314159.0"
      PI_EXTERNAL_RATE: "314.159"
      PI_INTERNAL_MULTIPLIER: "1000.0"
      CREDIT_SANDBOX: "true"
      CREDIT_GOVERNANCE_MODE: ${CREDIT_GOVERNANCE_MODE:-nesara_gesara}
      GLOBAL_CREDIT_PROVIDERS: ${GLOBAL_CREDIT_PROVIDERS:-equifax,experian,transunion,fico,vantagescore}
      # ── Founder Sovereign Identity ─────────────────────────────────────
      TRIUMPH_FOUNDER_NAME: "Jeremiah Joel Drains"
      TRIUMPH_FOUNDATION_ORG: "Triumph-Synergy Digital Financial Ecosystem"
      TRIUMPH_FOUNDER_AUTHORITY: "owner-approved"
      TRIUMPH_FOUNDER_ADDRESS: "GA6Z5STFJZPBDQT5VZSDUTCKLXXB626ONTLRWBJAWYKLH4LKPIZCGL7V"
      TRIUMPH_FOUNDER_TITLE: "Sovereign Owner and Creator"
      TRIUMPH_FOUNDER_SOVEREIGN_STATUS: "NESARA_GESARA_COMPLIANT"
      # Prefer local Horizon for all credit ledger tracking
      PI_LOCAL_HORIZON: http://host.docker.internal:31401
      # ── On-Chain Anchoring (Pi Testnet Blockchain) ──────────────────────
      # Keypair: GCVQFYCM7JYAH2KTNG2LDS422ASECKRVOJ7READD3IB3MLWN5BUL2SBL
      # Fund at: https://friendbot.testnet2.minepi.com?addr=GCVQFYCM7JYAH2KTNG2LDS422ASECKRVOJ7READD3IB3MLWN5BUL2SBL
      ONCHAIN_ANCHOR_SEED: "SBP5FZRJNT66SHHFSRLXMK2PZK3EZKUHEVPPY75I7Y5DTK4SMZHPGF2A"
      # ── Bureau OAuth2 Credentials (wire for live data) ──────────────────
      # Wire live bureau keys in production:
      # EQUIFAX_CLIENT_ID:         ${EQUIFAX_CLIENT_ID}
      # EQUIFAX_CLIENT_SECRET:     ${EQUIFAX_CLIENT_SECRET}
      # EXPERIAN_CLIENT_ID:        ${EXPERIAN_CLIENT_ID}
      # EXPERIAN_CLIENT_SECRET:    ${EXPERIAN_CLIENT_SECRET}
      # TRANSUNION_CLIENT_ID:      ${TRANSUNION_CLIENT_ID}
      # TRANSUNION_CLIENT_SECRET:  ${TRANSUNION_CLIENT_SECRET}
      # FICO_CLIENT_ID:            ${FICO_CLIENT_ID}
      # FICO_CLIENT_SECRET:        ${FICO_CLIENT_SECRET}
      # BUREAU_API_KEY_EQUIFAX:    ${BUREAU_API_KEY_EQUIFAX}
      # BUREAU_API_KEY_EXPERIAN:   ${BUREAU_API_KEY_EXPERIAN}
      # BUREAU_API_KEY_TRANSUNION: ${BUREAU_API_KEY_TRANSUNION}
      # BUREAU_API_KEY_FICO:       ${BUREAU_API_KEY_FICO}
      # BUREAU_API_KEY_VANTAGE:    ${BUREAU_API_KEY_VANTAGE}
    expose:
      - "8091"
    networks:
      - triumph-net
    depends_on:
      redis:
        condition: service_healthy
      ml-engine:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8091/health"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 30s

  # ========================================================================
  # QUANTUM SHIELD ENGINE — Post-quantum cryptographic resistance layer
  # NIST FIPS 203 (Kyber-1024) | FIPS 204 (Dilithium-5) | FIPS 205 (SPHINCS+)
  # Protects all 22 services against Shor + Grover quantum attacks
  # ========================================================================

  quantum-shield:
    build:
      context: docker/quantum-shield
    container_name: triumph-quantum-shield
    <<: *common
    mem_limit: 256m
    mem_reservation: 128m
    cpus: 0.25
    environment:
      PORT: "8094"
      REDIS_URL: redis://triumph-redis:6379
      # Canonical Pi dual-value constants
      PI_INTERNAL_RATE: "314159.0"
      PI_EXTERNAL_RATE: "314.159"
      PI_INTERNAL_MULTIPLIER: "1000.0"
      PI_SUPERNODE_ADDRESS: GA6Z5STFJZPBDQT5VZSDUTCKLXXB626ONTLRWBJAWYKLH4LKPIZCGL7V
      # Service mesh URLs for full ecosystem audit
      APP_URL: http://triumph-nginx:80
      MARKET_DATA_URL: http://triumph-market-data:8085
      BLOCKCHAIN_ORACLE_URL: http://triumph-blockchain-oracle:8086
      COMPLIANCE_URL: http://triumph-compliance:8087
      DEX_URL: http://triumph-dex:8088
      TOKENIZATION_URL: http://triumph-tokenization-engine:8089
      ML_ENGINE_URL: http://triumph-ml-engine:8090
      CREDIT_ENGINE_URL: http://triumph-credit-engine:8091
      PI_BRIDGE_URL: http://triumph-pi-bridge-connector:8092
      DUAL_VALUE_URL: http://triumph-dual-value-engine:8093
      CLOUD_MEMORY_URL: http://triumph-cloud-memory:8095
      TRANSACTION_URL: http://triumph-transaction-engine:8080
      PAYMENT_URL: http://triumph-payment-processor:8084
      VAULT_URL: http://triumph-vault:8081
      CONTRACTS_URL: http://triumph-smart-contracts:8082
      SCP_URL: http://triumph-scp-upgrader:8083
      QPU_BRIDGE_URL: http://triumph-qpu-bridge:8098
      SOVEREIGN_GATEWAY_URL: http://triumph-sovereign-gateway:8097
      BACKBONE_INTERVAL_S: "5"
      BACKBONE_TIMEOUT_S: "3"
      BACKBONE_MAX_FAILURES: "3"
      BACKBONE_ENDPOINTS: "http://triumph-pi-bridge-connector:8092/health,http://triumph-smart-contracts:8082/health,http://triumph-central-node:11626/health"
    expose:
      - "8094"
    networks:
      - triumph-net
    depends_on:
      redis:
        condition: service_healthy
      dual-value-engine:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8094/health', timeout=10)"]
      interval: 30s
      timeout: 20s
      retries: 5
      start_period: 90s          # extended: quantum-shield initializes CRYSTALS-Kyber key gen, slow under CPU pressure

  # ========================================================================
  # CLOUD MEMORY PLATFORM — LZ4 compressed, deduplicated, multi-platform store
  # 60-80% smaller payloads | content-addressed dedup | cross-service broadcast
  # Sustains 22+ platforms within one unified memory layer
  # ========================================================================

  cloud-memory:
    build:
      context: docker/cloud-memory
    container_name: triumph-cloud-memory
    <<: *common
    mem_limit: 256m
    mem_reservation: 96m
    cpus: 0.2
    user: "1000:1000"
    environment:
      PORT: "8095"
      REDIS_URL: redis://triumph-redis:6379
      DEFAULT_TTL_S: "3600"
      HOT_TTL_S: "300"
      WARM_TTL_S: "3600"
      COLD_TTL_S: "86400"
      LZ4_LEVEL: "9"
      AUTO_SYNC_ENABLED: ${CLOUD_MEMORY_AUTO_SYNC_ENABLED:-true}
      BACKUP_SCHEDULE_S: ${CLOUD_MEMORY_BACKUP_SCHEDULE_S:-900}
      BACKUP_LOCAL_DIR: /backup/local
      BACKUP_OFFLINE_DIR: /backup/offline
      CLOUD_MEMORY_S3_ENABLED: ${CLOUD_MEMORY_S3_ENABLED:-false}
      CLOUD_MEMORY_S3_BUCKET: ${CLOUD_MEMORY_S3_BUCKET:-}
      CLOUD_MEMORY_S3_PREFIX: ${CLOUD_MEMORY_S3_PREFIX:-triumph-cloud-memory}
      CLOUD_MEMORY_S3_REGION: ${CLOUD_MEMORY_S3_REGION:-us-east-1}
      CLOUD_MEMORY_S3_ENDPOINT: ${CLOUD_MEMORY_S3_ENDPOINT:-}
      CLOUD_MEMORY_S3_ACCESS_KEY_ID: ${CLOUD_MEMORY_S3_ACCESS_KEY_ID:-}
      CLOUD_MEMORY_S3_SECRET_ACCESS_KEY: ${CLOUD_MEMORY_S3_SECRET_ACCESS_KEY:-}
      APPLE_CLOUD_WEBDAV_ENABLED: ${APPLE_CLOUD_WEBDAV_ENABLED:-false}
      APPLE_CLOUD_WEBDAV_BASE_URL: ${APPLE_CLOUD_WEBDAV_BASE_URL:-}
      APPLE_CLOUD_WEBDAV_USERNAME: ${APPLE_CLOUD_WEBDAV_USERNAME:-}
      APPLE_CLOUD_WEBDAV_PASSWORD: ${APPLE_CLOUD_WEBDAV_PASSWORD:-}
      APPLE_CLOUD_WEBDAV_PATH: ${APPLE_CLOUD_WEBDAV_PATH:-/triumph-cloud-memory}
      SYNC_HTTP_TIMEOUT_S: ${CLOUD_MEMORY_SYNC_HTTP_TIMEOUT_S:-20}
      PI_INTERNAL_RATE: "314159.0"
      PI_EXTERNAL_RATE: "314.159"
      PI_INTERNAL_MULTIPLIER: "1000.0"
    volumes:
      - triumph_cloud_memory_backup:/backup
    expose:
      - "8095"
    networks:
      - triumph-net
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8095/health')"]
      interval: 20s
      timeout: 10s
      retries: 4
      start_period: 90s

  # ========================================================================
  # SOVEREIGN GATEWAY — Universal Integration Gateway
  # The single entry point ALL external systems (banks, governments,
  # exchanges, SWIFT, ACH, FedWire, Ethereum, Bitcoin) MUST connect
  # through to access Pi Network liquidity and the Triumph Synergy
  # ecosystem.  Pi is the base settlement layer; USD is derivative.
  # ========================================================================

  sovereign-gateway:
    build:
      context: docker/sovereign-gateway
    container_name: triumph-sovereign-gateway
    <<: *common
    mem_limit: 192m
    mem_reservation: 96m
    cpus: 0.2
    environment:
      PORT: "8097"
      REDIS_URL: redis://triumph-redis:6379
      PI_INTERNAL_RATE: "314159.0"
      PI_EXTERNAL_RATE: "314.159"
      PI_INTERNAL_MULTIPLIER: "1000.0"
      DUAL_VALUE_URL: http://triumph-dual-value-engine:8093
      PAYMENT_URL: http://triumph-payment-processor:8084
      CENTRAL_NODE_URL: http://triumph-central-node:11626
      PI_BRIDGE_URL: http://triumph-pi-bridge-connector:8092
      QUANTUM_SHIELD_URL: http://triumph-quantum-shield:8094
    expose:
      - "8097"
    networks:
      - triumph-net
    depends_on:
      redis:
        condition: service_healthy
      dual-value-engine:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8097/health')"]
      interval: 20s
      timeout: 10s
      retries: 4
      start_period: 15s

  # ========================================================================
  # DUAL-VALUE ENGINE — Pi Internal (mined) vs External (market) value
  # ========================================================================

  dual-value-engine:
    build:
      context: docker/dual-value-engine
    container_name: triumph-dual-value-engine
    <<: *common
    mem_limit: 128m
    mem_reservation: 64m
    cpus: 0.2
    environment:
      PORT: "8093"
      ML_ENGINE_URL: http://triumph-ml-engine:8090
      MARKET_DATA_URL: http://triumph-market-data:8085
      PI_BRIDGE_URL: http://triumph-pi-bridge-connector:8092
      CREDIT_ENGINE_URL: http://triumph-credit-engine:8091
      REDIS_URL: redis://triumph-redis:6379
      REFRESH_INTERVAL_S: "15"
      # CANONICAL Pi Dual-Value Constants — from lib/pios/pios-integration.ts
      PI_INTERNAL_RATE: "314159.0"       # $314,159/Pi — sovereign/mined (1000x)
      PI_EXTERNAL_RATE: "314.159"        # $314.159/Pi — market/traded
      PI_INTERNAL_MULTIPLIER: "1000.0"   # 1000x multiplier
      CLOUD_MEMORY_URL: http://triumph-cloud-memory:8095
      KYC_PREMIUM_PCT: "0.05"
      PIONEER_PREMIUM_PCT: "0.03"
    expose:
      - "8093"
    networks:
      - triumph-net
    depends_on:
      redis:
        condition: service_healthy
      ml-engine:
        condition: service_healthy
      market-data:
        condition: service_healthy
      pi-bridge-connector:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8093/health')"]
      interval: 20s
      timeout: 10s
      retries: 5
      start_period: 30s

  # ========================================================================
  # TOKENIZATION ENGINE — Pi domain + allodial deed tokenization
  # ========================================================================

  tokenization-engine:
    build:
      context: .
      dockerfile: docker/tokenization-engine/Dockerfile
    container_name: triumph-tokenization-engine
    <<: *common
    mem_limit: 128m
    mem_reservation: 64m
    cpus: 0.2
    environment:
      <<: [*db-env, *pi-env]
      NODE_OPTIONS: "--max-old-space-size=96"
      DATABASE_URL: postgresql://postgres:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD required}@triumph-postgres:5432/${POSTGRES_DB:-triumph_synergy}
      TOKENIZATION_HMAC_KEY: ${TOKENIZATION_HMAC_KEY:-triumph-tokenization-fortress-2026}
      ML_ENGINE_URL: http://triumph-ml-engine:8090
      CREDIT_ENGINE_URL: http://triumph-credit-engine:8091
      DUAL_VALUE_URL: http://triumph-dual-value-engine:8093
      QUANTUM_SHIELD_URL: http://triumph-quantum-shield:8094
      TOKENIZATION_REQUIRE_PQ_SIGNATURE: "true"
    expose:
      - "8089"
    networks:
      - triumph-net
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      credit-engine:
        condition: service_healthy
      ml-engine:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8089/health"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 25s

  # ========================================================================
  # PROMETHEUS — Metrics collection
  # ========================================================================

  prometheus:
    image: prom/prometheus:latest
    container_name: triumph-prometheus
    <<: *common
    mem_limit: 128m
    mem_reservation: 64m
    cpus: 0.2
    ports:
      - "9090:9090"
    volumes:
      - ./docker/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - ./docker/prometheus/alerts.yml:/etc/prometheus/alerts.yml:ro
      - triumph_prometheus_data:/prometheus
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--storage.tsdb.path=/prometheus"
      - "--storage.tsdb.retention.time=7d"
      - "--storage.tsdb.retention.size=256MB"
      - "--web.console.libraries=/usr/share/prometheus/console_libraries"
      - "--web.console.templates=/usr/share/prometheus/consoles"
      - "--web.enable-lifecycle"
    networks:
      - triumph-net
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:9090/-/healthy"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 15s

  # ========================================================================
  # GRAFANA — Metrics dashboards
  # ========================================================================

  # ========================================================================
  # POSTGRES EXPORTER — exposes pg metrics for Prometheus
  # ========================================================================
  postgres-exporter:
    image: prometheuscommunity/postgres-exporter:latest
    container_name: triumph-postgres-exporter
    <<: *common
    mem_limit: 32m
    mem_reservation: 16m
    cpus: 0.1
    environment:
      DATA_SOURCE_NAME: "postgresql://postgres:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD required}@triumph-postgres:5432/${POSTGRES_DB:-triumph_synergy}?sslmode=disable"
    expose:
      - "9187"
    networks:
      - triumph-net
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:9187/metrics"]
      interval: 20s
      timeout: 12s
      retries: 3
      start_period: 15s

  # ========================================================================
  # REDIS EXPORTER — exposes redis metrics for Prometheus
  # ========================================================================
  redis-exporter:
    image: oliver006/redis_exporter:alpine
    container_name: triumph-redis-exporter
    <<: *common
    mem_limit: 24m
    mem_reservation: 12m
    cpus: 0.1
    environment:
      REDIS_ADDR: "redis://triumph-redis:6379"
    expose:
      - "9121"
    networks:
      - triumph-net
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:9121/metrics"]
      interval: 20s
      timeout: 5s
      retries: 3
      start_period: 15s

  # ========================================================================
  # HORIZON GUARDIAN — keeps testnet2 running, Horizon alive, and
  # pi-bridge network connected for the entire Triumph ecosystem
  # ========================================================================
  horizon-guardian:
    build:
      context: docker/horizon-guardian
    container_name: triumph-horizon-guardian
    <<: *common
    restart: on-failure:10            # override: stop looping if Pi Node desktop is offline
    mem_limit: 128m
    mem_reservation: 32m
    cpus: 0.15
    environment:
      TARGET_CONTAINER: testnet2
      TARGET_PROCESS: horizon
      TARGET_URL: http://localhost:8000/
      GUARD_INTERVAL_S: "20"
      METRICS_PORT: "9911"
      PI_BRIDGE_NETWORK: pi-bridge
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    expose:
      - "9911"
    networks:
      - triumph-net
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:9911/health', timeout=8)"]
      interval: 30s
      timeout: 20s
      retries: 5
      start_period: 90s

  # ========================================================================
  # JUDICIAL MONITOR — Courtroom Transparency & Anti-Railroading Engine
  # Florida-first deployment — charge stacking, Brady violations,
  # emotional language detection, immutable transparency ledger
  # ========================================================================
  judicial-monitor:
    build:
      context: .
      dockerfile: docker/judicial-monitor/Dockerfile
    container_name: triumph-judicial-monitor
    <<: *common
    mem_limit: 128m
    mem_reservation: 64m
    cpus: 0.2
    environment:
      <<: [*db-env, *pi-env]
      SERVICE_NAME: judicial-monitor
      SERVICE_PORT: "8096"
      JUDICIAL_JURISDICTION: florida
    ports:
      - "8096:8096"
    networks:
      - triumph-net
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8096/health"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 15s

  grafana:
    image: grafana/grafana:latest
    container_name: triumph-grafana
    <<: *common
    mem_limit: 128m
    mem_reservation: 64m
    cpus: 0.2
    ports:
      - "3001:3000"
    environment:
      GF_SECURITY_ADMIN_USER:     ${GRAFANA_USER:?GRAFANA_USER required}
      GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_PASSWORD:?GRAFANA_PASSWORD required}
      GF_SERVER_ROOT_URL:         "%(protocol)s://%(domain)s:%(http_port)s/"
      GF_ANALYTICS_REPORTING_ENABLED: "false"
    volumes:
      - triumph_grafana_data:/var/lib/grafana
      - ./docker/grafana/datasources:/etc/grafana/provisioning/datasources:ro
      - ./docker/grafana/dashboards:/etc/grafana/provisioning/dashboards:ro
    depends_on:
      prometheus:
        condition: service_healthy
    networks:
      - triumph-net
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3000/api/health"]
      interval: 15s
      timeout: 12s
      retries: 8
      start_period: 90s

  # ========================================================================
  # HEALTH GOVERNOR — Lightweight memory watchdog for entire ecosystem
  # Restarts containers approaching mem_limit before they OOM-crash WSL2
  # ========================================================================
  health-governor:
    build:
      context: docker/health-governor
    container_name: triumph-health-governor
    <<: *common
    mem_limit: 64m
    mem_reservation: 24m
    cpus: 0.15
    environment:
      GOVERNOR_INTERVAL_S: "30"
      MEMORY_WARN_PCT: "80"
      MEMORY_KILL_PCT: "101"
      METRICS_PORT: "9912"
      CONTAINER_PREFIX: "triumph-"
      PROTECTED_CONTAINERS: "triumph-postgres,triumph-redis"
      HEALTH_FAIL_THRESHOLD: "3"
      DOCKER_UNHEALTHY_THRESHOLD: "3"
      RESTART_BACKOFF_BASE_S: "60"
      RESTART_BACKOFF_MAX_S: "600"
      HEALTH_PROBE_TIMEOUT_S: "10"
      ML_SELF_HEAL_ENABLED: "true"
      ML_HISTORY_WINDOW_S: "900"
      ML_MAX_RESTARTS_PER_WINDOW: "4"
      ML_SUPPRESSION_S: "300"
      CONTROL_PLANE_REQUIRE_PQ_READY: "true"
      QUANTUM_SHIELD_URL: "http://triumph-quantum-shield:8094"
      PQ_CHECK_INTERVAL_S: "30"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    expose:
      - "9912"
    networks:
      - triumph-net
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:9912/health', timeout=8)"]
      interval: 30s
      timeout: 12s
      retries: 3
      start_period: 10s

  # ========================================================================
  # NETWORK SENTINEL — Multi-WAN monitor & connection resilience
  # Detects Starlink/broadband/cellular transitions, verifies external
  # connectivity, auto-restarts services after network changes
  # ========================================================================
  network-sentinel:
    build:
      context: docker/network-sentinel
    container_name: triumph-network-sentinel
    <<: *common
    restart: on-failure:10            # override: don't crash-loop on network probe failures
    mem_limit: 48m
    mem_reservation: 16m
    cpus: 0.1
    environment:
      PROBE_INTERVAL_S: "15"
      METRICS_PORT: "9913"
      REDIS_URL: "redis://triumph-redis:6379"
      PI_NODE_HOST: "testnet2"
      PI_NODE_PEER_PORT: "31402"
      PI_HORIZON_URL: "http://testnet2:8000"
      PI_BRIDGE_URL: "http://triumph-pi-bridge-connector:8092"
      ML_ENGINE_URL: "http://triumph-ml-engine:8090"
      SELF_HEAL_ML_ENABLED: "true"
      ANOMALY_Z_THRESHOLD: "2.5"
      ANOMALY_STREAK_THRESHOLD: "3"
      CONTROL_PLANE_REQUIRE_PQ_READY: "true"
      QUANTUM_SHIELD_URL: "http://triumph-quantum-shield:8094"
      PQ_CHECK_INTERVAL_S: "30"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    expose:
      - "9913"
    networks:
      - triumph-net
      - pi-bridge
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:9913/health', timeout=10)"]
      interval: 30s
      timeout: 20s
      retries: 5
      start_period: 90s

  # ========================================================================
  # QPU BRIDGE — Quantum Processing Unit Bridge
  # Superior computational bridge connecting all 17+ ecosystem services.
  # SUPERPOSITION: true parallelism via asyncio.gather (all states at once)
  # ENTANGLEMENT:  bidirectional Redis pub/sub state sync between any pair
  # CIRCUIT:       multi-gate pipelines — SIGN|FORWARD|VERIFY|COMPUTE|FAN-OUT
  # TUNNEL:        every forward optionally quantum-signed (Dilithium-5)
  # ========================================================================
  qpu-bridge:
    build:
      context: docker/qpu-bridge
    container_name: triumph-qpu-bridge
    <<: *common
    mem_limit: 192m
    mem_reservation: 96m
    cpus: 0.2
    environment:
      PORT: "8098"
      REDIS_URL: redis://triumph-redis:6379
      QUANTUM_SHIELD_URL: http://triumph-quantum-shield:8094
      PI_INTERNAL_RATE: "314159.0"
      PI_EXTERNAL_RATE: "314.159"
      PI_INTERNAL_MULTIPLIER: "1000.0"
      PI_SUPERNODE_ADDRESS: GA6Z5STFJZPBDQT5VZSDUTCKLXXB626ONTLRWBJAWYKLH4LKPIZCGL7V
      APP_URL: http://triumph-nginx:80
      MARKET_DATA_URL: http://triumph-market-data:8085
      BLOCKCHAIN_ORACLE_URL: http://triumph-blockchain-oracle:8086
      COMPLIANCE_URL: http://triumph-compliance:8087
      DEX_URL: http://triumph-dex:8088
      TOKENIZATION_URL: http://triumph-tokenization-engine:8089
      ML_ENGINE_URL: http://triumph-ml-engine:8090
      CREDIT_ENGINE_URL: http://triumph-credit-engine:8091
      PI_BRIDGE_URL: http://triumph-pi-bridge-connector:8092
      DUAL_VALUE_URL: http://triumph-dual-value-engine:8093
      CLOUD_MEMORY_URL: http://triumph-cloud-memory:8095
      TRANSACTION_URL: http://triumph-transaction-engine:8080
      PAYMENT_URL: http://triumph-payment-processor:8084
      VAULT_URL: http://triumph-vault:8081
      CONTRACTS_URL: http://triumph-smart-contracts:8082
      SCP_URL: http://triumph-scp-upgrader:8083
      SOVEREIGN_GATEWAY_URL: http://triumph-sovereign-gateway:8097
    expose:
      - "8098"
    networks:
      - triumph-net
    depends_on:
      redis:
        condition: service_healthy
      quantum-shield:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8098/health', timeout=10)"]
      interval: 20s
      timeout: 12s
      retries: 5
      start_period: 30s

# ==========================================================================
# VOLUMES — Persistent Data
# ==========================================================================

volumes:
  triumph_tokenization_data:
    name: triumph_tokenization_data
  triumph_pg_data:
    name: triumph_pg_data
  triumph_redis_data:
    name: triumph_redis_data
  triumph_central_data:
    name: triumph_central_data
  triumph_stellar_data:
    name: triumph_stellar_data
  triumph_tx_data:
    name: triumph_tx_data
  triumph_vault_data:
    name: triumph_vault_data
  triumph_contracts_data:
    name: triumph_contracts_data
  triumph_scp_data:
    name: triumph_scp_data
  triumph_prometheus_data:
    name: triumph_prometheus_data
  triumph_grafana_data:
    name: triumph_grafana_data
  triumph_cloud_memory_backup:
    name: triumph_cloud_memory_backup

# ==========================================================================
# NETWORK — Shared with Pi Network node for direct communication
# ==========================================================================
#
# After starting:  docker network connect pi-bridge testnet2
# This connects the Pi Node container to the ecosystem so central-node
# can communicate with it directly.
# ==========================================================================

networks:
  triumph-net:
    name: triumph-net
    driver: bridge
  pi-bridge:
    name: pi-bridge
    driver: bridge

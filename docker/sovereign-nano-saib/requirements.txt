fastapi>=0.122.0
starlette>=1.0.1
uvicorn>=0.37.0
cryptography>=46.0.7
httpx>=0.28.1
prometheus-client>=0.21.1
pydantic>=2.10.4
anyio>=4.9.0
numpy>=1.26.4,<3
websockets>=14.1
networkx>=3.3
scipy>=1.13.0
# v3 connector layer
aiofiles>=23.2.1
# v4 apex engines
aiohttp>=3.13.4
# v5 sovereign apex engines
PyJWT>=2.12.0
# v5 billing (Pi + USD Stripe)
stripe>=7.0.0
# v6 Omega Prime — container introspection
docker>=7.0.0
# v8 OMNI SOVEREIGN — LLM Brain (Gemini + OpenRouter free tiers)
google-generativeai>=0.8.0
# v8 OMNI SOVEREIGN — Market Oracle (stocks/bonds via Yahoo Finance)
yfinance>=0.2.51
# v8 OMNI SOVEREIGN — Resource Sovereign host metrics
psutil>=6.1.0
# Note: GEMINI_API_KEY and OPENROUTER_API_KEY are injected at runtime via docker-compose env vars
# v9 OMNI MASTER SOVEREIGN — Multi-agent orchestration (LangGraph)
langgraph>=0.2.0
langchain-core>=0.3.0
# v9 GitHub Synergy Engine — repo clone + execute loop
gitpython>=3.1.0
# v9 GCV Ledger — PostgreSQL async driver
asyncpg>=0.29.0
